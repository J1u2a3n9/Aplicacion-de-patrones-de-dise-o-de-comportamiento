using McNutResources.Notifier;
using System;

namespace McNutResources
{
    class Program
    {
        static void Main(string[] args)
        {
            PrincipalMainNotifier menu = new PrincipalMainNotifier();
            menu.LaunchPrincipalMenu();

        }
    }
}
